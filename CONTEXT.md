# 听障实时字幕展示系统

为听障人士在面对面多人对话中提供实时字幕的桌面应用。把流式语音识别输出的原文交给 LLM 整理后，按说话人显示为彩色气泡与随机头像，并在会话结束后生成会议纪要。

## Language

### 实时字幕

**说话人 (Speaker)**:
对话中的一位发言者，由说话人切换检测在实时中区分，系统以"说话人 1/2/3"自动编号，用户可手动重命名。
_Avoid_: 发言人（指正式职位场合）、人员（歧义）

**气泡 (Bubble)**:
一条发言的可视化容器，按所属说话人着色，展示文字与随机头像。
_Avoid_: 消息框、卡片

**原文 (Raw Transcript)**:
语音识别引擎输出的、未经整理的转写文字。不可变，是整理的输入与最终事实来源。
_Avoid_: 转写稿、原始文本

**整理版 (Cleaned Text)**:
LLM 对原文去口语化、纠错、补标点后的文字，是界面默认展示的形态。
_Avoid_: 润色文本、规整结果

**双轨展示 (Dual-track Display)**:
原文与整理版并行保留、可切换查看的展示方式。默认只显示整理版，用户可一键切换显示原文。
_Avoid_: 原地替换（已被否决的做法）

**会话 (Session)**:
从开始识别到停止识别的一次完整过程，包含期间产生的全部原文片段与整理结果。
_Avoid_: 会议（指真实的线下会议，易混淆）、录制

**会议纪要 (Meeting Minutes)**:
会话停止后，由 LLM 把整理版文本分批汇总生成的结构化总结（要点/行动项/待办）。
_Avoid_: 摘要（指更短的总结）

**说话人切换检测 (Speaker Change Detection, SCD)**:
实时判断相邻两段发言是否来自同一说话人的机制，基于声纹向量余弦匹配。
_Avoid_: 说话人分离、声纹识别（分别指全自动聚类与身份识别，属不同能力）

### 配置

**本地 ASR / 云端 ASR (Local / Cloud ASR)**:
两种可切换的语音识别来源：本地离线运行，云端经网络调用。同一抽象接口，设置中可切换。
_Avoid_: 引擎（单独使用时歧义）

**整理间隔 (Cleanup Interval)**:
把已冻结原文片段送去 LLM 整理的周期，设置中可选（建议 5s/10s 档位）。
_Avoid_: 刷新率（指屏幕刷新频率）

**置顶大字模式 (Always-on-top Focus Mode)**:
主窗口之外的显示形态：始终置顶 + 超大字体，供对话中持续盯看字幕使用。
_Avoid_: 悬浮窗（指独立小窗，另一形态）
